/**
 * Test fixtures for consistent test data across all test suites
 */
export * from './categories'

// Export commonly used fixtures at the top level for convenience
export {
  categoriesArrays,
  categoriesComponentProps,
  categoryFixtures,
  createCategoriesArray,
  createCategoryFixture,
  permissionFixtures,
} from './categories'
